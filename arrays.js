const colors = [];
colors.push('purple', 'red', 'pink');

const numbers = [2,4,6,8,10];
colors.pop();
colors.unshift('yellow');

console.log('Colors array length:', colors.length);
console.log('Numbers array length:', numbers.length);